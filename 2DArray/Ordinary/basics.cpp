#include<iostream>
#include<vector>
using namespace std;
int main(){
    // vector<vector<int> > v;
    // vector<vector<int> > v(m,);
    // vector<vector<int> > v(m,vector<int> (n)); //m rows n colomn
    // vector<vector<int> > v(m,vector<int> (n,k)); // m rows n column k in each
    // pop back allowed
}