#include<iostream>
#include<algorithm>
using namespace std;
int main(){
    // // str.size() = size || str.length()
    string str = "My name is karan kumar sah";
    // cout << str.size() << endl;
    // cout << str.length() << endl;


    // // str.push_back('x') = add x to last
    // // str.pop_back() = delete last word
    // cout << str << endl;
    // str.push_back('e');
    // cout << str << endl;
    // str.pop_back();
    // cout << str << endl;


    // // "+" oprator =add string in order;
    // string s = "abc";
    // string t = "def";
    // s = "xyz" + s;
    // cout << s + t << endl;


   //  //reverse(str.begin()+starting index, str.begin()+ending index + 1);
    // reverse(str.begin()+1, str.begin()+2);
    // cout << str << endl;


    // // str.substr(intex, length);
    // cout << str.substr(1, 3);


    //to_string() //convert integer to string
    // int x = 12345;
    // string str = to_string(x);
    // cout << str;


    // sort(str.begin(), str.end()); // according to ascii value (words also)
    // space number special capital small


    // // stoi(string to int) vs stoll(string to long long)
    // // int -> -2^(31) to 2^(31) - 1
    // // long long -> -2^(63) to 2^(63) - 1
    // int x = stoi(str);
    // cout << x << endl;
    // long long y = stoll(str);
    // cout << y << endl;
}